import pyjokes

print(pyjokes.get_joke())

# to use python as a calculator we type python on cmd then it opens the python terminal which is basically a REPL (read evaluate print loop) in which we gives the expressions as input and its gives its answer as output